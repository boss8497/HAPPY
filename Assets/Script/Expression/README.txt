수식 계산용 파일입니다.

예를 들어 `(level + 2) * 10` 과 같은 표현식을 처리하기 위해 사용됩니다.
이 기능을 사용하려면 `ValueStringKey.StringKeyCache`에 필요한 문자열 키의 Index가 사전에 등록되어 있어야 합니다.

수식은 문자열 자체를 그대로 유지하지 않고, 문자열 사용량을 줄이기 위해 내부적으로 토큰 단위로 분해되어 저장됩니다.
런타임에서는 해당 토큰 데이터를 기반으로 별도의 문자열 파싱 과정 없이 계산을 수행합니다.